[project]
name = "audit-infrastructure"
authors = ["Stacks Developer"]
description = "A decentralized funding and milestone verification platform"
telemetry = false
requirements = []
[contracts.milestone-auditor]
path = "contracts/milestone-auditor.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
